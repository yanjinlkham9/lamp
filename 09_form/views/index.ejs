<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>form 전송</title>
  </head>
  <body>
    <h2>GET 요청</h2>
    <!-- action: 요청 주소 
         method: ilgeeh arga (get or post)
    -->
    <form action="/getForm" method="get">
      <label> id: <input type="text" name="id" /> </label>
      <br />
      <label> email: <input type="email" name="email" /> </label>
      <br />
      <label> password: <input type="password" name="pw" /> </label>
      <!-- button default type: submit -->
      <button type="button">제출?</button>
      <button type="submit">제출</button>
    </form>
    <h2>POST 요청</h2>
    <form action="/postForm" method="post">
      <label for="id">ID</label>
      <!-- required: 필수 속성 시 써줌 -->
      <input
        type="text"
        name="id"
        id="id"
        placeholder="ID 작성해주세요"
        required
        title="아이디는 대소문자, 숫자, 한글로 구성된 네글자 이상의 문자여야 합니다"
        pattern="^([a-zA-Z0-9가-힣]){4,}$"
      />
      <br />
      <label for="pw">password</label>
      <input
        type="password"
        name="pw"
        id="pw"
        placeholder="비밀번호 입력"
        title="비밀번호는 영어소문자, 숫자로 구성된 8-12글자의 문자여야 함니다"
        required
        pattern="^([a-z0-9]){8,12}$"
      />
      <br />
      <!-- checkbox-uudiin name n ijilhen baij baij neg label gj medegdene -->
      <label>
        <input type="checkbox" name="agree" value="marketing" />
        마케팅동의
      </label>
      <label>
        <input type="checkbox" name="agree" value="private" />
        개인정보 수집 동의
      </label>
      <label>
        <input type="checkbox" name="agree" value="use" />
        이용약관 동의
      </label>
      <br />
      <input type="submit" value="제출" />
    </form>

    <h2>유효성 검사</h2>
    <form
      action="/js-form-check"
      method="post"
      name="validationForm"
      target="_blank"
      onsubmit="return checkForm(this)"
    >
      ID:
      <input
        type="text"
        name="id2"
        title="최소 한 글자"
        minlength="1"
        placeholder="최소한 글자를 작성"
        id="input"
      />
      <br />
      <span>우편물 수령 장소</span>
      <label> <input type="checkbox" name="mail" value="자택" />자택 </label>
      <label> <input type="checkbox" name="mail" value="회사" />회사 </label>
      <br />
      <button>제출</button>
    </form>
    <br />
    <br /><br />
    <hr />
    <h3>실습문제 페이지로 이동</h3>
    <a href="/practice1">PRACTICE1</a>
    <a href="/practice2">PRACTICE2</a>
    <script>
      function checkForm(form) {
        // this - 나 자신을 의미, form 태그 그 자체
        // const form2 = document.forms["validationForm"]
        console.log(form);
        console.log(form.id2); //선택된폼.name
        console.log(form.input); //선택된폼.id
        if (form.id2.value.trim() === "") {
          alert("id를 입력해주세요");

          // return false를 해야만 submit 이벤트를 막을 수 있음
          return false;
        }

        //   checkbox 하나라도 선택되어야 제출되도록
        console.log(form.mail); //배열형태
        //checkbox 상태가 둘다 false라면 return:false
        if (form.mail[0].checked == false && form.mail[1].checked == false) {
          alert("우편물 수령 장소를 선택해주세요");
          return false; //event 막기
        }
      }
    </script>
  </body>
</html>
